import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["cdn.pixabay.com", "pix8.agoda.net", "cf.bstatic.com", "q-xx.bstatic.com", "images.pexels.com"]
  }
};

export default nextConfig;
